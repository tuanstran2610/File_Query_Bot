# sql_server_query_tool.py
def query_data(query, llm):
    # Logic giả lập
    # sql = llm.invoke(f"Tạo câu SQL cho: {query}").content
    # return f"SQL executed: {sql} - Result: [data]"
    return ('query')